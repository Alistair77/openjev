# Safety and abstention

Treat low confidence, incomplete choices, ambiguous evidence, backend failures, and missing calibration as reasons to abstain or escalate. Never convert a model response directly into shell commands, browser actions, money movement, access changes, or high-impact decisions. Validate action targets independently and keep the raw trace.
